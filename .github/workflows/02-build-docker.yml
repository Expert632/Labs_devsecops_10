---
name: 02 - Docker Build

on:
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

jobs:
  docker-build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout du repo
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Setup Docker
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 3️⃣ Build Docker image
      - name: Build Docker image
        run: |
          docker build -t devsecops-lab:latest .

      # 4️⃣ Optional: Tag Docker image (local)
      - name: Tag Docker image
        run: |
          docker tag devsecops-lab:latest devsecops-lab:1.0

      # 5️⃣ Optional: Push to Docker Hub (requires secrets DOCKER_USERNAME, DOCKER_PASSWORD)
      #- name: Login to Docker Hub
      #  uses: docker/login-action@v2
      #  with:
      #    username: ${{ secrets.DOCKER_USERNAME }}
      #    password: ${{ secrets.DOCKER_PASSWORD }}

      #- name: Push Docker image
      #  run: docker push devsecops-lab:1.0
